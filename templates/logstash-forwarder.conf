{
  "network": {
    "servers": [ "{{ elk_logserver_ip_address }}:5000" ],
    "timeout": 15,
    "ssl ca": "{{ logstash_client_ssl_certificate_fullpath }}"
  },
  "files":
    {{ logstash_client_combined_logfiles | to_nice_json }}
}

